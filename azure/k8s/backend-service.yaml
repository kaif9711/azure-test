apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: backend
  ports:
  - protocol: TCP
    port: 80       # The port the service will be available on
    targetPort: 8000 # The port your container is running (from the deployment)
  type: ClusterIP # Internal-only IP