// Copy this file to config.js to override defaults.
// Example:
// window.FRAUD_API_BASE_URL = 'http://localhost:8000';
// window.FRAUD_FEATURE_FLAGS = { enableRealtime: true };

// Intentionally left without active assignments so it does not override when copied blindly.
